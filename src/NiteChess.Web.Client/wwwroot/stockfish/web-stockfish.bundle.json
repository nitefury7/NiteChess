{
  "hostId": "web-client",
  "integrationMode": "BrowserWasmWorker",
  "bundleRoot": "wwwroot/stockfish",
  "workerEntryPoint": "wwwroot/workers/stockfish.worker.js",
  "engineScript": "wwwroot/stockfish/stockfish.js",
  "engineWasm": "wwwroot/stockfish/stockfish.wasm",
  "requiredAssets": [
    "wwwroot/workers/stockfish.worker.js",
    "wwwroot/stockfish/stockfish.js",
    "wwwroot/stockfish/stockfish.wasm"
  ],
  "notes": "Serve the tracked worker plus the locally provisioned Stockfish 18 JS/WASM assets as static web files so offline browser sessions evaluate locally without any server round-trip. The large `stockfish.wasm` payload is intentionally gitignored so the repo stays pushable without Git LFS."
}