{
  "hostId": "desktop",
  "integrationMode": "NativeProcess",
  "bundleRoot": "Assets/Stockfish/native",
  "entryPointPattern": "Assets/Stockfish/native/{rid}/stockfish",
  "requiredAssets": [
    "Assets/Stockfish/native/osx-arm64/stockfish",
    "Assets/Stockfish/native/osx-x64/stockfish",
    "Assets/Stockfish/native/linux-x64/stockfish"
  ],
  "notes": "Desktop resolves locally provisioned Stockfish 18 executables from this asset tree for macOS arm64, macOS x64, and Linux x64. Those large runtime files are intentionally gitignored so the repo stays pushable without Git LFS; desktop still copies any present local payloads into the app output."
}